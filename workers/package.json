{
	"name": "workers",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env workers",
		"dev": "wrangler dev --env workers",
		"start": "wrangler dev --env workers",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.7.5",
		"wrangler": "^3.101.0",
		"vitest": "~3.0.7"
	}
}
